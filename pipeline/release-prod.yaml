steps:
- name: gcr.io/google.com/cloudsdktool/cloud-sdk
  entrypoint: gcloud
  args:
    [
       "deploy", "releases", "promote", "--delivery-pipeline",
       "genai-sw-delivery", "--release", "ledgerwriter-fix-pr${_PR_NUMBER}", 
       "--region", "europe-west1"
    ]

